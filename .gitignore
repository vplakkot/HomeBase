# dependencies
/node_modules

# next.js
/.next/
/out/

# testing
/coverage

# production
/build

# misc
.DS_Store
*.pem

# git worktrees the Claude Code desktop app creates inside the repo. Ignored
# here so it's true for every clone, not just whichever machine happened to
# add it to .git/info/exclude.
.claude/worktrees/

# local env files
.env*.local
.env

# typescript
*.tsbuildinfo
next-env.d.ts
.vercel
.env*
